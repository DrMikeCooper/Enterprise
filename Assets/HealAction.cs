using UnityEngine;
using System.Collections;

public class HealAction : Need {

	// Use this for initialization
	void Start () {
        Need[] needs = GetComponents<Need>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
